const initialState = {
  auth: undefined
};

export const user = (state = initialState, { type, payload }) => {
  switch (type) {
    default:
      return state;
  }
  return 2;
};

export default user;
